import { TestBed } from '@angular/core/testing';

import { AmBulbaService } from './am-bulba.service';

describe('AmBulbaService', () => {
  // let service: AmBulbaService;
  //
  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(AmBulbaService);
  // });
  //
  // it('should be created', () => {
  //   expect(service).toBeTruthy();
  // });
});
